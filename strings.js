// prompt the user to enter their full name .generate a user name for them based on thee input.
// start user name with '@' and end with the length of the user name;

let fullName = prompt("Enter you full name without spaces");
console.log("@"+fullName+fullName.length);